apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "containerinsights-geneva-logs.fullname" . }}-test-connection"
  labels:
    {{- include "containerinsights-geneva-logs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "containerinsights-geneva-logs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
