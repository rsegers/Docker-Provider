apiVersion: v1
kind: Service
metadata:
  name: {{ include "containerinsights-geneva-logs.fullname" . }}
  labels:
    {{- include "containerinsights-geneva-logs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "containerinsights-geneva-logs.selectorLabels" . | nindent 4 }}
